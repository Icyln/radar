# Radar implementation status

Date: 2026-08-13

## Completed

### Phase 0

Complete.

### Phase 1

Complete and verified by the user against a real Cloudflare Greenhouse board and local PostgreSQL.

### Phase 2

Implemented:

- Lever collector
- Ashby collector
- shared normalized pipeline
- User
- JobProfile
- deterministic matching engine
- JobMatch uniqueness
- saved/ignored unified user-job state
- TelegramConnection
- hashed, expiring, single-use TelegramLinkToken
- per-user Notification identity
- stale `SENDING` recovery policy
- monitoring priorities in manual worker
- baseline-safe match persistence and alert behavior

### Phase 3

Implemented:

- Argon2 password authentication
- signed expiring JWT access tokens
- current-user and administrator dependencies
- ownership enforcement
- job-profile CRUD
- matched/saved/ignored jobs API
- job state updates
- dashboard summary endpoint
- company list/admin create/admin update
- Telegram link-token endpoint
- Telegram connection status/disconnect
- Telegram webhook secret verification
- `/start <token>` account linking
- Save/Ignore callback handling
- health/readiness endpoints retained

### Phase 4

Implemented:

- register/login UI
- server-set HttpOnly auth cookie
- Next.js Route Handler API proxy
- authenticated dashboard shell
- responsive desktop/mobile navigation
- dashboard operational summary
- recent matches
- profile CRUD/toggle UI
- matched/saved/ignored jobs UI
- active/unknown/closed lifecycle filtering
- save/ignore state actions
- company monitoring table
- administrator company creation and enable/pause controls
- account settings
- Telegram linking/status/disconnect UI
- loading, error, and empty states

No database migration is required for Phase 4.

## Frontend lint correction

`frontend/next-env.d.ts` is generated by Next.js. The repository now ignores it in ESLint so Next.js-generated route type references do not fail `npm run lint`.

## Database migration

Phase 2/3 migration:

```text
0002_phase2_phase3
```

It upgrades an existing Phase 0/1 database in place. Existing companies/jobs/crawler logs/Phase-1 notifications are retained.

## Automated verification in the build environment

- Python compilation: pass
- pytest: 18 tests pass
- Alembic PostgreSQL offline SQL generation through `0002_phase2_phase3`: pass
- frontend dependency installation was unavailable in the build environment; syntax/static source checks were performed, and local `npm run lint`, `npm run typecheck`, and `npm run build` remain required acceptance checks

## External ATS contracts used

- Greenhouse public Job Board API: existing Phase 1 integration
- Lever public Postings API: `GET /v0/postings/{SITE}` JSON mode
- Ashby public Job Postings API: `GET /posting-api/job-board/{JOB_BOARD_NAME}`

Provider-specific payloads are normalized into the same `NormalizedJob` model.

## Current phase boundary

Not implemented yet:

- Phase 5 GitHub Actions high/normal/discovery schedules and CI expansion for production monitoring
- Phase 6 discovery/hardening

The Render API remains outside the monitoring critical path.
