name: Source Discovery

on:
  schedule:
    # Refresh active-hiring discovery every six hours, record health, and deliver new broad-search matches.
    # The midnight run also refreshes Himalayas; its public JSON data refreshes daily.
    - cron: "23 0 * * *"
    - cron: "23 6 * * *"
    - cron: "23 12 * * *"
    - cron: "23 18 * * *"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: radar-source-discovery
  cancel-in-progress: false

jobs:
  discover:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    defaults:
      run:
        working-directory: backend
    env:
      ENVIRONMENT: worker
      LOG_LEVEL: INFO
      DATABASE_URL: ${{ secrets.DATABASE_URL }}
      TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
      DISCOVERY_RUN_TRIGGER: github-actions
      DISCOVERY_EXTERNAL_RUN_ID: ${{ github.run_id }}.${{ github.run_attempt }}
      TELEGRAM_MAX_NOTIFICATIONS_PER_RUN: "100"
      DISCOVERY_SYSTEM_FEED_URLS: ${{ vars.DISCOVERY_SYSTEM_FEED_URLS }}
      DISCOVERY_HIRING_ARBEITNOW_ENABLED: ${{ vars.DISCOVERY_HIRING_ARBEITNOW_ENABLED != 'false' }}
      DISCOVERY_HIRING_ARBEITNOW_PAGES: "4"
      DISCOVERY_HIRING_HIMALAYAS_PAGES: "3"
      # Himalayas documents a 24-hour data refresh; avoid redundant rate-limited queries.
      DISCOVERY_HIRING_HIMALAYAS_ENABLED: ${{ github.event_name == 'workflow_dispatch' || github.event.schedule == '23 0 * * *' }}

    steps:
      - name: Check out repository
        uses: actions/checkout@v7

      - name: Set up Python
        uses: actions/setup-python@v7
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: backend/pyproject.toml

      - name: Install Radar discovery worker
        run: python -m pip install --disable-pip-version-check -e .

      - name: Validate database configuration
        run: python -m app.scripts.check_worker_config --require-remote-database

      - name: Discover and validate ATS sources
        run: >-
          python -m app.workers.discovery
          --target-batch-size 25
          --candidate-batch-size 50
          --max-concurrency 3
          --revalidate-batch-size 50
          --auto-promote
          --ingest-system-feeds
          --ingest-hiring-signals
          --revalidate-promoted
